# Database Configuration
# Replace these with your actual Render PostgreSQL database credentials
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=your_database_host
DB_PORT=5432

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# Note: Do not commit the actual .env file to version control
# This is just a template - create a real .env file with your actual credentials